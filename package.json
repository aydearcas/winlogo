{
  "name": "winlogo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel serve src/index.html",
    "deploy": "rm -rf build && parcel build src/index.html -d build && gh-pages -d build --public-url /winlogo/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.20.0",
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "stylelint": "^13.10.0",
    "stylelint-config-standard": "^20.0.0"
  }
}
